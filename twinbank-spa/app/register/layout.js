export const metadata = {
  title: "Login | TwinBank",
  description: "Registrate en TwinBank",
  keywords: "transacciones, twinbank, banco, digital, dinero, transferencias, pagos, depositos, retiros, retiro, deposito, pago, transferencia, transaccion",
}

export default function RegisterLayoutV2({ children }) {
  return (
      <>
          {children}
      </>

  )
}